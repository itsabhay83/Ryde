import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const Ride = () => {
  return (
    <SafeAreaView>
      <Text> History </Text>
    </SafeAreaView>
  );
};
export default Ride;
